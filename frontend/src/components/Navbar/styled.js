import styled from 'styled-components';

export const Navbar = styled.nav`
	padding: 1em 4%;
	display: flex;
	align-items: center;
	background-color: black;

	a {
		margin: 0 1em;
		color: white;
		font-weight: bold;
	}
`